<ivysettings>
  <settings defaultResolver="chain"/>
  <property name="ivy.retrieve.pattern" value="[artifact]-[revision](-[classifier]).[ext]" override="false"/>
  <resolvers>
    <chain name="chain">
      <ibiblio name="central" m2compatible="true"/>
    </chain>
  </resolvers>
</ivysettings>
